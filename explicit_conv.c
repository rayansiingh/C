#include <stdio.h>

int main()
{
    // explicit type conversion
    // this isn't throwing error but to conv type we use
    // int a = (int) 1.999999;
    int a = 1.999999;
    printf("%d \n", a);
    return 0;
}